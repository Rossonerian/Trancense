export function Donut({ value, label }: { value: number; label: string }) { const dash = Math.max(0, Math.min(100, value)) * 2.51; return <div style={{display:"flex",alignItems:"center",gap:15}}><svg width="82" height="82" viewBox="0 0 100 100" role="img" aria-label={`${value}% ${label}`}><circle cx="50" cy="50" r="40" fill="none" stroke="#464646" strokeWidth="12" /><circle cx="50" cy="50" r="40" fill="none" stroke="var(--cyan)" strokeWidth="12" strokeDasharray={`${dash} ${251-dash}`} strokeLinecap="round" transform="rotate(-90 50 50)" /><text x="50" y="55" textAnchor="middle" fill="var(--cloud)" fontSize="17" fontWeight="600">{value}%</text></svg><div><div className="card-title">{label}</div><div className="card-subtitle">Evidence-led completeness<br />across 12 reporting months</div></div></div>; }
